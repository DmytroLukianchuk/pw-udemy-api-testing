// import { test, expect } from "@playwright/test";
// import { articles, baseUrl, apiCall } from "../MOCK/data";

// test("'My first mock test", async ({ page }) => {
//   await page.route(apiCall, async (route) => {
//     await route.fulfill({
//       body: JSON.stringify(articles),
//     });
//   });
//   await page.goto(baseUrl);
//   expect(page.getByRole("heading", { name: "All Articles" })).toBeDisabled();
// });
